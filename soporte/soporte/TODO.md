- [ ] Unificar paquetes de Spring moviendo `SoporteApplication` a `com.soporte.soporte`
- [ ] Implementar `TicketSoporteRepository` como `interface` que extiende `JpaRepository` y agregar método `findByIdUsuario`
- [ ] Asegurar imports/paquetes consistentes en `TicketSoporteService` y `TicketSoporteController`
- [ ] Ejecutar `mvn test` y/o `mvn spring-boot:run` para validar compilación/arranque


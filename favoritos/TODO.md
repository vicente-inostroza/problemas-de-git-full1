# TODO - Arreglar errores (favoritos)

- [ ] Verificar y unificar paquetes: `com.example.favoritos` vs `com.favoritos.favoritos`.
- [ ] Corregir imports/`package` de todas las clases para que coincidan con `FavoritosApplication` (base package).
- [ ] Ejecutar `mvn test` y confirmar que el ApplicationContext carga.
- [ ] (Opcional) Mejorar manejo de errores de `RestTemplate` al llamar al catálogo.

